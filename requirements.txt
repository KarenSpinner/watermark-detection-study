# Core (running and analyzing the API grids)
anthropic>=0.40
google-genai>=0.3
openai>=1.40
numpy>=1.24
python-dotenv>=1.0

# Validation step only (cloud_validate.py, needs a GPU)
# torch>=2.2
# transformers>=4.46      # SynthIDTextWatermarkingConfig
# accelerate>=0.30
